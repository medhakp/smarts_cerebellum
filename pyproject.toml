[project]
name = "smarts_cerebellum"
version = "0.0.1"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["smarts_cerebellum"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
